<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_menu_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.prateekjoshi.agri_app.MenuScreen"
    android:background="@color/white">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/orchard"
        android:scaleType="centerCrop"
        android:maxHeight="300dp"
        android:maxWidth="300dp"
        android:id="@+id/menu_image"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="TU AGRI CONTROL"
        android:layout_below="@+id/menu_image"
        android:id="@+id/menu_header"
        android:textColor="@color/gray"
        android:textSize="30dp"
        android:layout_marginLeft="60dp"/>
   <ImageButton
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:src="@drawable/ic_yourprofile"
       android:layout_alignParentBottom="true"
       android:background="@color/white"/>


</RelativeLayout>
